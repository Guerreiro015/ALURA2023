const idade = 16
const limite = 18
if(idade >=limite){
    console.log('Já pode beber cerveja')
}
else{
    console.log('não pode beber, e muito criança')
}

// A mesma coisa usando OPERADOR TENÁRIO fazemos a operação direto

console.log(idade >=limite? 'pode ternario' : 'não pode ternário')

